import {
  currentIteration,
  currentState,
  currentReverse,
  easingFunc,
  finished,
  iterationsComplete,
  paused,
  previousIteration,
} from './animation';

import { filter } from './misc';
import match from './match';

export {
  currentIteration,
  currentState,
  currentReverse,
  easingFunc,
  filter,
  finished,
  iterationsComplete,
  match,
  paused,
  previousIteration,
};
