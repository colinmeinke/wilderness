import {
  currentIteration,
  currentState,
  currentReverse,
  easingFunc,
  finished,
  iterationsComplete,
  paused,
  previousIteration
} from './animation'

import { filter } from './misc'
import match from './match'
import normalise from './points'
import tween from './tween'

export {
  currentIteration,
  currentState,
  currentReverse,
  easingFunc,
  filter,
  finished,
  iterationsComplete,
  match,
  normalise,
  paused,
  previousIteration,
  tween
}
